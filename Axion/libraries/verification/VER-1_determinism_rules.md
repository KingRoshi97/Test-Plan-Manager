---
library: verification
id: VER-1a
schema_version: 1.0.0
status: draft
---

# VER-1a — Determinism Rules (Proof Types)

- proof_type is a closed enum and must exist in the registry.
- required_fields are enforced by proof ledger validation.
- Registry is pinned per run (for reproducibility).
