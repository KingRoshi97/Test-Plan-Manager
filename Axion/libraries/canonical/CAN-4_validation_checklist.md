---
library: canonical
id: CAN-4c
schema_version: 1.0.0
status: draft
---

# CAN-4c — Validation Checklist

- [ ] unknown_assumptions artifact validates against schema
- [ ] every item has pointer + severity + prompt
- [ ] builder defaults are recorded as assumptions (no silent defaults)
- [ ] ordering is deterministic (severity → pointer → id)
