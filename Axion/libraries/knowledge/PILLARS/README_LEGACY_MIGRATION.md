# PILLARS — Legacy Directory (FROZEN)

This directory tree is **frozen** as of 2026-03-11.

All canonical knowledge items have been migrated to `CONTENT/ITEMS/` with registry-based metadata.

## What this means

- **No new content** should be added to PILLARS/.
- **No existing files** should be edited here — edit the canonical copy in `CONTENT/ITEMS/{kid}.md`.
- The resolver uses `SYSTEM/registries/` for lookup, with PILLARS/ as **fallback only**.
- This tree will be removed once all legacy path references have been redirected.

## Migration references

- Canonical items: `CONTENT/ITEMS/`
- Collection descriptors: `CONTENT/META/collections/`
- Alias registry: `SYSTEM/registries/aliases.index.json`
- Relationship registry: `SYSTEM/registries/relationships.index.json`
- Knowledge index: `SYSTEM/registries/knowledge.index.json`
