# FE-03 — UI State Model

## 1. Header Block

| Field             | Value                                              |
|-------------------|----------------------------------------------------|
| Template ID       | FE-03                                            |
| Template Type     | Build / Frontend                                 |
| Template Version  | 1.0.0                                            |
| Applies           | All projects requiring ui state model            |
| Filled By         | Internal Agent                                   |
| Consumes          | Canonical Spec, Standards Snapshot               |
| Produces          | Filled UI State Model                            |

## 2. Purpose

Define the canonical state management architecture for the frontend: stores/slices, shape, data sources, derived state, persistence, and data-flow rules. This template must be consistent with screens and components and must not introduce state not driven by upstream inputs.

## 3. Inputs Required

- SPEC_INDEX: {{spec.index}}
- DOMAIN_MAP: {{domain.map}} | OPTIONAL
- GLOSSARY: {{glossary.terms}} | OPTIONAL
- STANDARDS_INDEX: {{standards.index}} | OPTIONAL
- FE-01 Screen Catalog: {{fe.screen_catalog}}
- FE-02 Component Catalog: {{fe.component_catalog}}
- API-01 Endpoint Catalog: {{api.endpoint_catalog}} | OPTIONAL
- Existing docs/notes: {{inputs.notes}} | OPTIONAL

## 4. Required Fields

| Field Name | Source | UNKNOWN Allowed |
|---|---|---|
| Store/slice registry (store_id list) | spec | No |
| Store name | spec | No |
| State shape (key/type pairs) | spec | No |
| Data source (API/local/derived) | spec | No |
| Mutation actions | spec | No |
| Selectors/derived state | spec | Yes |
| Persistence policy | spec | Yes |
| Scope (global/screen/component) | spec | Yes |
| Screen/component bindings | FE-01/FE-02 | No |

## 5. Optional Fields

| Field Name | Source | Notes |
|---|---|---|
| Cache/TTL policy | spec | OPTIONAL |
| Optimistic update policy | spec | OPTIONAL |
| Middleware/side-effects | spec | OPTIONAL |
| DevTools integration | — | OPTIONAL |
| Open questions | — | OPTIONAL |

## 6. Rules

- Must align to: {{standards.rules[STD-CANONICAL-TRUTH]}} | OPTIONAL
- Every store_id MUST be unique.
- All state MUST be traceable to a screen or component (or flagged as UNKNOWN).
- Do not invent API endpoints not present in {{xref:API-01}}.
- If a required field is not provided by inputs, write UNKNOWN unless explicitly disallowed.
- Use consistent terminology from: {{glossary.terms}} | OPTIONAL

## 7. Cross-References

- **Upstream**: {{xref:FE-01}}, {{xref:FE-02}}, {{xref:API-01}} | OPTIONAL, {{xref:SPEC_INDEX}} | OPTIONAL
- **Downstream**: {{xref:FE-04}}, {{xref:FE-06}} | OPTIONAL
- **Standards**: {{standards.rules[STD-NAMING]}} | OPTIONAL, {{standards.rules[STD-UNKNOWN-HANDLING]}} | OPTIONAL

## 8. Skill Level Requiredness Rules

| Skill Level | Required | Notes |
|---|---|---|
| beginner | Required | List stores with shape and data source; selectors/persistence UNKNOWN ok. |
| intermediate | Required | Define mutation actions, selectors, and persistence policy. |
| advanced | Required | Add cache/TTL, optimistic updates, middleware/side-effects. |

## 9. Unknown Handling

- UNKNOWN_ALLOWED: domain.map, glossary.terms, scope, selectors, persistence, cache_ttl, optimistic_updates, component_bindings, middleware, flow_description, open_questions
- If any Required Field is UNKNOWN, allow only if: {{standards.rules[STD-UNKNOWN-HANDLING]}} | OPTIONAL
- If store_id list is UNKNOWN → block Completeness Gate.
- If state_shape is UNKNOWN → block Completeness Gate.

## 10. Completeness Gate

- Gate ID: TMP-05.PRIMARY.FE
- Pass conditions:
  - [ ] required_fields_present == true
  - [ ] all store_ids are unique
  - [ ] screen_bindings bind to valid screen_ids
  - [ ] placeholder_resolution == true
  - [ ] no_unapproved_unknowns == true

## 11. Output Format

