# FE-02 — Component Implementation Spec

## 1. Header Block

| Field             | Value                                              |
|-------------------|----------------------------------------------------|
| Template ID       | FE-02                                             |
| Template Type     | Build / Frontend                                          |
| Template Version  | 1.0.0                                              |
| Applies           | All projects requiring component implementation spec    |
| Filled By         | Internal Agent                                     |
| Consumes          | Canonical Spec, Intake Submission, Standards Snapshot |
| Produces          | Filled Component Implementation Spec Document                         |

## 2. Purpose

Define the canonical specification format for implementing frontend components, including
props/slots/variants, state handling, accessibility requirements, data binding hooks,
theming/token usage, and test/acceptance references. This template must be consistent with
composition patterns and must not invent component IDs not present in upstream inputs.

## 3. Inputs Required

- SPEC_INDEX: {{spec.index}}
- DOMAIN_MAP: {{domain.map}} | OPTIONAL
- GLOSSARY: {{glossary.terms}} | OPTIONAL
- STANDARDS_INDEX: {{standards.index}} | OPTIONAL
- FE-01 Route Map + Layout Spec: {{fe.route_layout}} | OPTIONAL
- UICP-02 Component Composition Rules: {{ui.component_composition}} | OPTIONAL
- FE-06 Theming/Tokens Integration: {{ui.theming}} | OPTIONAL
- FORM-02 Field Spec: {{forms.field_specs}} | OPTIONAL
- Existing docs/notes: {{inputs.notes}} | OPTIONAL

## 4. Required Fields

component_id (must exist in SPEC_INDEX)
component_name (human-readable)
purpose/usage
variants (variant_id list)
props contract (name/type/required/default)
slots/children contract (if applicable)
state model (loading/empty/error/success)
interaction behavior (events/callbacks)
a11y requirements (roles, labels, focus)
styling/token binding rules (theme tokens)
data dependencies (queries/mutations) if any
error handling behavior (bind to FE-07/CER)
test hooks / acceptance refs

Optional Fields
Performance notes | OPTIONAL
Analytics hooks | OPTIONAL
i18n notes | OPTIONAL
Open questions | OPTIONAL
Rules
Must align to: {{standards.rules[STD-CANONICAL-TRUTH]}} | OPTIONAL
Do not introduce new component_ids; use only {{spec.components_by_id}}.
Component behavior MUST align with UI composition rules ({{xref:UICP-02}}) and theming rules
({{xref:FE-06}}).
If a required field is not provided by inputs, write UNKNOWN unless explicitly disallowed.
Use consistent terminology from: {{glossary.terms}} | OPTIONAL
If component touches forms, it MUST align with {{xref:FORM-02}} and {{xref:FORM-03}}.
Output Format
1. Component Identity
component_id: {{component.component_id}}
name: {{component.name}}
purpose: {{component.purpose}}
where_used: {{component.where_used}} | OPTIONAL
2. Variants
variant
variant_id: {{variants[0].variant_id}}
name: {{variants[0].name}}
when_to_use: {{variants[0].when_to_use}}
visual_notes: {{variants[0].visual_notes}} | OPTIONAL
(Repeat for each variant.)
3. Props Contract
prop
name: {{props[0].name}}
type: {{props[0].type}}
required: {{props[0].required}}
default: {{props[0].default}} | OPTIONAL
description: {{props[0].description}} | OPTIONAL
(Repeat for each prop.)
4. Slots / Children (Optional)
slots: {{slots}} | OPTIONAL
children_rules: {{children.rules}} | OPTIONAL

5. State Model
states_supported: {{state.states_supported}} (loading/empty/error/success/UNKNOWN)
loading_behavior: {{state.loading_behavior}} | OPTIONAL
empty_behavior: {{state.empty_behavior}} | OPTIONAL
error_behavior: {{state.error_behavior}}
success_behavior: {{state.success_behavior}} | OPTIONAL
6. Interaction Behavior
events:
{{interaction.events[0]}}
{{interaction.events[1]}} | OPTIONAL
callbacks: {{interaction.callbacks}} | OPTIONAL
keyboard_interactions: {{interaction.keyboard}} | OPTIONAL
7. Accessibility (A11y)
roles: {{a11y.roles}}
labels: {{a11y.labels}}
focus_order: {{a11y.focus_order}} | OPTIONAL
aria_props: {{a11y.aria_props}} | OPTIONAL
8. Styling / Tokens
token_usage: {{style.token_usage}}
variants_token_rules: {{style.variants_token_rules}} | OPTIONAL
responsive_rules: {{style.responsive_rules}} | OPTIONAL
9. Data Dependencies (Optional)
queries: {{data.queries}} | OPTIONAL
mutations: {{data.mutations}} | OPTIONAL
cache_invalidation: {{data.cache_invalidation}} | OPTIONAL
10.Error Handling
error_surface_rules: {{errors.surface_rules}} (bind to {{xref:FE-07}}/{{xref:CER-01}}) |
OPTIONAL
retry_behavior: {{errors.retry_behavior}} | OPTIONAL
11.Tests / Acceptance
test_ids: {{tests.test_ids}} | OPTIONAL
acceptance_refs: {{tests.acceptance_refs}} | OPTIONAL
12.References
Route/layout context: {{xref:FE-01}} | OPTIONAL
Composition rules: {{xref:UICP-02}} | OPTIONAL
Theming/tokens: {{xref:FE-06}} | OPTIONAL
Forms: {{xref:FORM-02}} | OPTIONAL
Client error UX: {{xref:FE-07}} | OPTIONAL
Cross-References
Upstream: {{xref:SPEC_INDEX}} | OPTIONAL, {{xref:FE-01}} | OPTIONAL
Downstream: {{xref:FE-03}}, {{xref:FE-04}} | OPTIONAL
Standards: {{standards.rules[STD-NAMING]}} | OPTIONAL,

{{standards.rules[STD-UNKNOWN-HANDLING]}} | OPTIONAL, {{standards.rules[STD-A11Y]}} |
OPTIONAL
Skill Level Requiredness Rules
beginner: Required. Use UNKNOWN where prop/variant details are missing; do not invent
component_ids.
intermediate: Required. Define state model, a11y, styling tokens, and interaction events.
advanced: Required. Add data dependency hooks, performance notes, and test/acceptance
traceability.
Unknown Handling
UNKNOWN_ALLOWED: domain.map, glossary.terms, where_used, visual_notes, prop
defaults/descriptions, slots/children rules, loading/empty/success behaviors, interaction
callbacks/keyboard, focus order/aria props, variants token rules, responsive rules, data deps,
error surface rules, tests, analytics/i18n notes, open_questions
If any Required Field is UNKNOWN, allow only if:
{{standards.rules[STD-UNKNOWN-HANDLING]}} | OPTIONAL
If component_id is UNKNOWN → block Completeness Gate.
If state.states_supported is UNKNOWN → block Completeness Gate.
If a11y.roles is UNKNOWN → block Completeness Gate.
Completeness Gate
Gate ID: TMP-05.PRIMARY.FE
Pass conditions:
required_fields_present == true
component_id exists in SPEC_INDEX (no new IDs introduced)
state_model_defined == true
a11y_requirements_defined == true
placeholder_resolution == true
no_unapproved_unknowns == true

FE-03

FE-03 — UI State Model (loading/empty/error/success)
Header Block

## 5. Optional Fields

Performance notes | OPTIONAL
Analytics hooks | OPTIONAL
i18n notes | OPTIONAL
Open questions | OPTIONAL

## 6. Rules

- Must align to: {{standards.rules[STD-CANONICAL-TRUTH]}} | OPTIONAL
- Do not introduce new component_ids; use only {{spec.components_by_id}}.
- **Component behavior MUST align with UI composition rules ({{xref:UICP-02}}) and theming rules**
- **({{xref:FE-06}}).**
- If a required field is not provided by inputs, write UNKNOWN unless explicitly disallowed.
- Use consistent terminology from: {{glossary.terms}} | OPTIONAL
- If component touches forms, it MUST align with {{xref:FORM-02}} and {{xref:FORM-03}}.

## 7. Output Format

### Required Headings (in order)

1. `## Component Identity`
2. `## Variants`
3. `## variant`
4. `## (Repeat for each variant.)`
5. `## Props Contract`
6. `## prop`
7. `## (Repeat for each prop.)`
8. `## Slots / Children (Optional)`
9. `## State Model`
10. `## Interaction Behavior`

## 8. Cross-References

- **Upstream: {{xref:SPEC_INDEX}} | OPTIONAL, {{xref:FE-01}} | OPTIONAL**
- **Downstream: {{xref:FE-03}}, {{xref:FE-04}} | OPTIONAL**
- **Standards: {{standards.rules[STD-NAMING]}} | OPTIONAL,**
- {{standards.rules[STD-UNKNOWN-HANDLING]}} | OPTIONAL, {{standards.rules[STD-A11Y]}} |
- OPTIONAL

## 9. Skill Level Requiredness Rules

| Section                    | Beginner  | Intermediate | Expert   |
|----------------------------|-----------|--------------|----------|
| Overview                   | Required  | Required     | Required |
| Core Specification         | Required  | Required     | Required |
| Detailed Fields            | Optional  | Required     | Required |
| Advanced Configuration     | Optional  | Optional     | Required |

## 10. Unknown Handling

- If a required field cannot be resolved from inputs, write `UNKNOWN` and add to Open Questions.
- UNKNOWN fields do not block gate passage unless explicitly marked `UNKNOWN Allowed: No`.
- All UNKNOWN entries must include a reason and suggested resolution path.

## 11. Completeness Gate

- All Required Fields must be populated or explicitly marked UNKNOWN with justification.
- Output must follow the heading structure defined in Section 7.
- No invented data — all content must trace to canonical spec or intake submission.
- Cross-references must resolve to valid template IDs.
