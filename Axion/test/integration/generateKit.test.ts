import { describe, it } from "vitest";

describe("integration: generateKit", () => {
  it.todo("should produce a complete kit from a valid submission");
  it.todo("should fail if intake validation fails");
  it.todo("should fail if gate checks fail");
  it.todo("should include all required artifacts in manifest");
});
