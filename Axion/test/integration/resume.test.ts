import { describe, it } from "vitest";

describe("integration: resume", () => {
  it.todo("should resume a paused run from state snapshot");
  it.todo("should skip completed stages on resume");
  it.todo("should re-run failed stages");
  it.todo("should produce consistent output regardless of resume point");
});
