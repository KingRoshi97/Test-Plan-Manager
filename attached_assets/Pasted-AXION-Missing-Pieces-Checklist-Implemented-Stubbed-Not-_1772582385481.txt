AXION Missing Pieces Checklist (Implemented / Stubbed / Not wired / Missing)

Status meanings:

Implemented = exists and is executed in the current run flow

Not wired = code exists but not executed by the main flow

Stubbed = placeholder/NotImplemented logic

Missing = not present as a real runtime surface

0) Foundations
Repo structure + runtime root

.axion/ runtime root creation: Implemented

run folder shape under .axion/runs/<run_id>/: Implemented

deterministic path conventions: Partially implemented (structure exists; full contract enforcement missing)

CLI control surface

core CLI entry + command routing: Implemented

stage-by-stage run execution (axion run step flow): Implemented

targeted stage runner (axion stage etc.): Present (Not wired consistently into a stage machine)

1) Intake (INTAKE)
Intake artifacts (submission → normalized → record)

intake schemas/contracts enforced: Missing

cmdValidateIntake (actual implementation): Stubbed

normalized input generation: Missing (as a real deterministic transform)

submission record with hashes/pointers: Missing

Net: INTAKE stage exists conceptually but is not real yet.

2) Canonical (CANONICAL)
Canonical spec generation

canonical spec builder: Stubbed

unknowns/assumptions artifact: Not wired

canonical diff artifact between runs: Missing

canonical schema enforcement: Missing

Net: canonical file exists today but is placeholder, not derived.

3) Standards (STANDARDS)
Registries + resolution

standards index/applicability/packs exist in docs, but runtime enforcement: Not wired / Missing

standards applicability evaluation output: Missing

deterministic constraint resolution (precedence/conflict rules): Not wired

resolved standards snapshot (real): Not wired (placeholder exists)

standards snapshot pinned into pinset: Missing

Net: standards snapshot exists as a file, but not as a resolved, pinned contract output.

4) Templates (TEMPLATES)
Template selection + render

template library exists: Implemented (content exists)

template selection result contract: Partially implemented / Not consistently enforced

render envelopes as primary output: Partially implemented

completeness rules enforced (no missing required placeholders): Missing / Not wired

placeholder/derive syntax validator: Missing

“rendered_docs” output strategy is causing bloat: Present (but not governed)

Net: templates exist and outputs are being written, but the enforcement and contract boundaries are not locked.

5) Planning (PLANNING)

plan generation (cmdPlanWork): Implemented

work breakdown artifact: Implemented

acceptance map artifact: Implemented

sequencing report artifact: Implemented

1-target-per-unit enforcement: Missing (contract exists in docs; enforcement not locked)

Net: planning stage is one of the most real parts today.

6) Gates (GATES)

gate execution (cmdRunGates): Implemented

per-gate report artifacts: Implemented

predicate operator registry enforcement (no invented operators): Missing

evidence policy mapping enforcement (gate → required proof types): Missing

pointer resolver enforcement (dereference + hash match rules): Missing

non-overridable gate list enforcement: Missing (policy not locked at runtime)

Net: gates run and produce outputs, but “gates as law” is not fully enforced yet.

7) Proof (PROOF)

proof ledger writing (cmdWriteProof): Implemented

proof type registry enforcement: Missing

proof completeness rules (PASS requires required proofs): Missing

proof-to-gate linkage enforcement: Missing

redaction rules for logs/proofs: Missing

Net: proof ledger exists, but proof governance is not enforced.

8) Kit (KIT)

kit packaging (cmdPackageKit): Implemented

kit manifest / entrypoint / version stamp: Implemented

internal vs external kit variant filtering: Missing

“kit must not contain secrets” scanner: Missing

reuse allowlist behavior enforced: Missing

kit validation step (KCP validator): Missing / Not wired

Net: kit artifacts are produced; safety and variant enforcement are not real yet.

9) State + Verify (STATE / VERIFY)

state snapshot + resume plan + handoff packet (cmdWriteState): Implemented

verification completion report (cmdVerify): Implemented (but currently more “reporting” than “verification engine”)

repro from manifest/pins with diffing: Not wired / Partially implemented

retention + archive manifest: Missing (policy exists on paper)

Net: state outputs exist; long-term reproducibility + retention needs hardening.

10) Run Profiles & Build Recipes (PROFILE layer)

run profile registry: Missing

profile resolution result artifact: Missing

effective config artifact (profile + mode overlay): Missing

mode/profile precedence rules enforced: Missing

Net: designed, but not implemented as runtime behavior yet.

11) Knowledge Library (KL)

file tree and KID lists (paper design): In progress (you’ve started)

knowledge index registry contract enforced: Missing

knowledge selection artifact per run: Missing

external vs internal knowledge filtering enforced: Missing

Net: design direction is clear; runtime coupling is not implemented yet.

12) CI + Conformance (TEST/CI)

golden fixtures per stage: Missing

negative test suites (“fail at correct stage with remediation”): Missing

end-to-end golden runs in CI: Missing

determinism normalization + diff checks: Missing

Net: this is required for AXION to be provably correct.

Highest-impact missing items (if you want a strict next build order)

Make INTAKE + CANONICAL real (no more placeholders)

Make STANDARDS snapshot real + pinned

Make TEMPLATE selection/render completeness enforceable

Lock gates as law (operator registry + evidence policy + pointer resolver)

Enforce proof requirements + redaction

Implement RUN_PROFILE resolution + effective config artifact

Retention policy to stop .axion/runs bloat