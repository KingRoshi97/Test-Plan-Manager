You are upgrading the Axiom web app UI to a premium, dark-first design while preserving light mode.
Stack: Vite + React + TypeScript + Tailwind + shadcn/ui + Wouter.
Routing currently exists. Core functionality exists (wizard, assemblies list, assembly detail, uploads, deliveries). This task is UI/UX overhaul + structure, not new backend features.

GOAL
Implement a sidebar-first application shell and refactor key screens into a cohesive premium product UI:
- Create (wizard)
- Assemblies (list + search/filters)
- Assembly Detail (tabs: Overview / Kit / Deliveries / Attachments)
- Settings (new page)
Keep /docs (API docs) accessible.

NON-NEGOTIABLES
- Do not break existing functionality or API calls.
- Preserve ThemeProvider and dark/light toggle. Dark-first polish is default.
- Reuse shadcn/ui primitives; create a small “kit” layer of styled wrappers for consistency.
- Add loading skeletons, empty states, copy buttons, and toasts.
- Navigation must be sidebar-based and consistent on all pages.

========================================
1) ROUTES + IA (Wouter)
========================================
Target routes (ensure they exist and render):
- /create                  -> Create wizard page
- /assemblies              -> Assemblies list page
- /assemblies/:assemblyId  -> Assembly detail page
- /settings                -> Settings page (new)
- /docs                    -> API docs page (existing)

Also keep backwards redirects:
- / -> /create
- /runs -> /assemblies
- /runs/:id -> /assemblies/:id

========================================
2) NEW LAYOUT: AppShell (SIDEBAR-FIRST)
========================================
Create an AppShell that wraps all pages:
- Left SidebarNav (collapsible if easy)
- TopBar (search slot + theme toggle + small actions)
- Main content area

Sidebar sections:
A) Primary nav
- Create
- Assemblies
- Settings
- Docs (or under secondary)

B) Quick actions
- “New Assembly” button -> /create
- “Upload” button -> opens Upload modal (calls uploads API, returns uploadId, shows status)

C) Small status indicator (optional)
- e.g., “Active Assemblies” count if available, or placeholder.

Use shadcn sidebar primitives if present, otherwise implement with Tailwind + Card.

========================================
3) DESIGN SYSTEM “KIT” (CREATE THESE COMPONENTS)
========================================
Create /client/src/components/kit/ with these components, used everywhere:

- GlassCard.tsx
  - Wraps shadcn Card with glass/elevated look:
    - subtle blur, border, shadow, rounded-2xl, background using CSS vars
- PageHeader.tsx
  - Title, subtitle, right-side actions slot
- StatusBadge.tsx
  - Maps states: queued/running/completed/failed/canceled to consistent badge styles
- DataTable.tsx
  - Thin wrapper around shadcn table with sticky header support
- CodeBlock.tsx
  - Monospace block with Copy button and optional language label
- CopyButton.tsx
  - Reusable copy-to-clipboard with toast feedback
- EmptyState.tsx
  - Icon + title + description + CTA button
- Skeletons.tsx
  - Skeleton variants for list rows, page blocks, code blocks
- Stepper.tsx
  - Wizard step indicator with validation state
- RunTimeline.tsx (rename to AssemblyTimeline)
  - Visual stepper for init/gen/seed/draft/review/verify/lock/package

Do not overbuild. Keep it clean and consistent.

========================================
4) PAGE OVERHAULS
========================================

----------------------------------------
4.1 Create Wizard (/create)
----------------------------------------
Refactor into a polished multi-step wizard:
Steps:
1) Basics (+ Uploads)
2) Features (table editor)
3) Users (list editor)
4) Tech (optional)
5) Review (JSON preview + Assemble button)

Requirements:
- Review step shows EXACT payload JSON to be sent.
- Uploads: drag/drop zone + file picker; show list with extraction status and warnings.
- Validation: inline; disable Next if required fields missing.
- Use Stepper component at top.
- “Assemble” triggers API call and routes to /assemblies/:assemblyId on success.
- On error, show toast + inline error banner with reason codes.

----------------------------------------
4.2 Assemblies List (/assemblies)
----------------------------------------
Upgrade into a dashboard table:
- PageHeader with “Assemblies” + “New Assembly” button
- Search input
- Filters (state, preset, date range, generationMode if present)
- Table columns:
  - Project Name
  - State (StatusBadge)
  - Created
  - Domains
  - Open Questions count
  - Actions: Open
- Loading: skeleton rows
- EmptyState: no assemblies -> CTA to /create

----------------------------------------
4.3 Assembly Detail (/assemblies/:assemblyId)
----------------------------------------
Implement a Tab layout:
Tabs:
- Overview
  - AssemblyTimeline (steps)
  - Progress / state
  - Logs tail viewer with Copy logs
  - Key metadata (generationMode, checksums)
- Kit
  - Download kit zip button
  - Copy signed URL button
  - Render Assembly Manifest JSON (pretty CodeBlock)
  - Render Agent Prompt markdown/text in CodeBlock
  - Show delivery/input.json and delivery/ai_context.json content when available
- Deliveries
  - “Create Delivery” modal (pull/webhook/git/direct)
  - Deliveries table: type, state, attempts, lastAttemptAt, lastError, Retry button
- Attachments
  - Uploads list with status + warnings
  - Text preview panel (extracted.txt)
  - Show original filename + checksum

Requirements:
- Robust loading states per tab.
- Copy buttons everywhere (URLs, JSON blocks, checksums).
- Errors show reason codes in a banner and toast.

----------------------------------------
4.4 Settings (/settings) (NEW)
----------------------------------------
Create a settings page with panels (even if some actions are stubbed):
- API Keys (list/create/rotate/revoke UI; wire up if endpoints exist)
- Rate Limits info (static display if needed)
- Webhook Test tool:
  - Generates sample payload + signature headers for bundle.ready
- Git Presets:
  - Save default repo/baseBranch/pathPrefix (local storage if backend not ready)

----------------------------------------
4.5 Docs (/docs)
----------------------------------------
Keep existing but re-style:
- Brand header shows “Assembler API”
- Use PageHeader + CodeBlocks for curl examples
- Sidebar link to Docs

========================================
5) FILE STRUCTURE (TARGET)
========================================
Create/organize files like:

client/src/
  app/
    AppShell.tsx
    SidebarNav.tsx
    TopBar.tsx
  pages/
    create.tsx
    assemblies.tsx
    assembly-detail.tsx (or folder)
    settings.tsx
    api-docs.tsx
  components/kit/
    GlassCard.tsx
    PageHeader.tsx
    StatusBadge.tsx
    DataTable.tsx
    CodeBlock.tsx
    CopyButton.tsx
    EmptyState.tsx
    Skeletons.tsx
    Stepper.tsx
    AssemblyTimeline.tsx
  lib/
    format/ (dates, bytes, status maps)

Keep existing shadcn components in components/ui.

========================================
6) POLISH PASS (MUST)
========================================
- Add skeleton loaders to Assemblies list and Assembly detail sections.
- Add EmptyState components for no data.
- Add toast feedback for copy actions and API success/fail.
- Ensure spacing is consistent (8px grid), cards are rounded-2xl, and typography hierarchy is clear.
- Dark mode looks premium by default; light mode remains readable and consistent.

========================================
7) ACCEPTANCE CHECKLIST
========================================
- Sidebar nav works across all pages.
- /create wizard is polished and includes Review JSON preview.
- /assemblies list has search + filters UI, skeletons, empty state, status badges.
- /assemblies/:assemblyId has tabs: Overview/Kit/Deliveries/Attachments with correct content areas.
- /settings page exists and is styled.
- Theme toggle works.
- No broken routes.
- No broken API calls.
- UI feels cohesive and premium.

DELIVERABLE
Commit the UI overhaul changes in a single PR/commit series with a clear message:
“UI overhaul: AppShell + Create/Assemblies/Detail/Settings + design kit”
