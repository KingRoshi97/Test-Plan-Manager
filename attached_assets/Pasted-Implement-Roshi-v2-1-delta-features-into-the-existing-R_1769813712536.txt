Implement Roshi v2.1 delta features into the existing Roshi v2 repo.

GOALS
Add repo hygiene extensions, protected paths enforcement, Model B reset behavior, terminology registry, domain build order system, expanded run sequences, and purge policy.

1) Add config
- Create roshi/protected-paths.json with protected patterns:
  server/db/schema.ts, server/db/client.ts, migrations/**,
  src/theme/**, src/assets/brand/**, assets/**,
  roshi/**, scripts/**
- Implement scripts/lib/protected-paths.mjs utilities:
  - loadProtectedPaths()
  - isProtected(path)
  - assertNoProtectedTouches(operations[]) fail-fast

2) Add new commands + scripts
- scripts/roshi-reorg.mjs
  - Move root clutter into _archive/legacy_misc/<timestamp>/...
  - Never touch protected paths
  - Output MOVE_REPORT.json and MOVE_REPORT.md
- scripts/roshi-consolidate.mjs
  - Consolidate attached_assets/** and scattered .md into docs/legacy/**
  - Produce required outputs:
    docs/legacy/INDEX.md
    docs/legacy/attached_assets.md
    docs/legacy/repo_docs/** categorized
- scripts/roshi-reset.mjs
  - Archive docs/roshi_v2/** to _archive/roshi_resets/<timestamp>/
  - Rebuild workspace skeleton
  - Restore Project Overview as canonical
  - Regenerate roshi/domains.json from Project Overview domain list (Model B)
  - Regenerate domain packs and registries
  - Support flag: --wipe-frontend
    - Archive src/** to _archive/legacy_frontend/<timestamp>/src/**
    - Preserve src/theme/**, src/assets/brand/**, assets/**
    - Leave minimal Rebuild Mode boot shell so app runs

3) Model B control plane rules
- Enforce overview-first:
  - roshi/sources.json must list Project Overview first
- Implement project overview parser in scripts/lib/project-overview-parse.mjs
  - deterministic parsing of domains list
  - if unknown/missing, write UNKNOWN + Open Questions and stop

4) Terminology registry
- Add docs/roshi_v2/00_registry/terminology.md template
- Update roshi:verify with Terminology Gate:
  - file exists
  - required headings exist (Roles, Domains, Entities, Statuses, Copy Keys)

5) Domain build order system
- Add docs/roshi_v2/00_registry/DOMAIN_BUILD_ORDER.md template
- Verify gate:
  - includes all domains from roshi/domains.json
  - prerequisites only reference valid domains
  - no cycles

6) Expanded run sequences + purge policy docs
- Add docs/roshi_v2/RUN_SEQUENCES.md with 4 sequences:
  new build bootstrap, existing rebuild (no wipe), existing rebuild (wipe), docs-only refresh
- Add docs/roshi_v2/00_registry/PURGE_POLICY.md
- Optional: implement roshi:purge with --dry-run

7) Wire npm scripts
- Add package.json scripts: roshi:reorg, roshi:consolidate, roshi:reset, roshi:purge

8) Testing/Acceptance
- Run each new command in --dry-run and normal mode
- Confirm fail-fast on protected path violations
- Confirm reset regenerates domains.json from Project Overview
- Confirm verify includes new gates and fails correctly when docs missing
- Each script prints a ROSHI_REPORT with created/modified/skipped/failed
