You’re basically on the right track, but there’s one critical correction before you implement anything:

Critical alignment fix: your stage IDs don’t match the Mechanics pipeline

In Axion Mechanics, the template stages are:

S6_SELECT_TEMPLATES → G4_TEMPLATE_SELECTION

S7_RENDER_DOCS → G5_TEMPLATE_COMPLETENESS

And S5_... is already used for standards (S5_RESOLVE_STANDARDS). If you introduce S5_TEMPLATE_SELECTION, you’ll create a naming collision with the spec’d pipeline order.

What to do

Implement exactly what you described, but map it to the Mechanics stage IDs:

Your “S5_TEMPLATE_SELECTION” implementation should actually be S6_SELECT_TEMPLATES

Your “S7_TEMPLATE_FILL” implementation should actually be S7_RENDER_DOCS

Everything else (files, reports, gates, placeholder rules) stays the same.

Refined implementation plan (your tasks, corrected to spec)
T001 — Add json_eq evaluator op ✅ keep

Add json_eq(path, pointer, expected) to src/core/gates/evaluator.ts

Failure code: E_VALUE_MISMATCH

Evidence includes actual vs expected

Update GateCheck typing to allow expected

✅ This is clean and needed for G5.

T002 — Add G4 + G5 to registries/GATE_REGISTRY.json ✅ keep, but fix stage_ids

Use:

G4_TEMPLATE_SELECTION → stage_id: S6_SELECT_TEMPLATES

G5_TEMPLATE_COMPLETENESS → stage_id: S7_RENDER_DOCS

Checks (good):

G4: file_exists + json_valid + json_has /selected_templates

G5: file_exists + json_valid + json_eq /unresolved_placeholders_count == 0

Acceptance: registry JSON valid; gate count increases by 2.

T003 — Template selector logic ✅ keep, but confirm index path

You wrote: “Load template_index.json from baseDir/libraries/templates/template_index.json”

In your repo right now, you already have:

registries/TEMPLATE_INDEX.json

So for MVP, pick one source of truth now:

Recommended: use registries/TEMPLATE_INDEX.json (since registries are what runtime reads)

Selection rule (MVP):

profile "default" selects templates where requiredness == "always" (your “8”)

Return:
{ template_id, path, version }

T004 — Renderer + placeholder scanner ✅ keep

Your regex + dotted path resolver is correct.

One improvement (important for determinism):

Implement scanUnresolvedPlaceholders(rendered_content) returning:

per-key occurrence counts

total unresolved count

You already planned that — good.

T005 — S6 evidence generator (selection_result + selection_report) ✅ keep, rename stage

Rename it in your stage wiring to:

writeSelectionResult = stage S6_SELECT_TEMPLATES output

Write files:

.axion/runs/<run_id>/templates/selection_result.json

.axion/runs/<run_id>/templates/selection_report.json

T006 — S7 evidence generator (rendered docs + render_report) ✅ keep, rename stage

Rename it in wiring to:

writeRenderedDocs = stage S7_RENDER_DOCS output

The one thing to simplify to guarantee MVP success (unresolved == 0)

You do not need a “smart context builder” yet.

Instead do this deterministic approach:

Read all selected template source files

Extract all placeholder keys across them (regex)

Build a context object that guarantees every key resolves:

For each key, set a value like "__AXION_VALUE__" (or "")

Optionally override some “nice” keys with real data:

run_id, generated_at

stuff from canonical spec / standards snapshot if present

This guarantees:

unresolved_placeholders_count == 0

G5 passes

you don’t spend time inventing domain mapping logic too early

Rendered outputs:

.axion/runs/<run_id>/templates/rendered_docs/<template_id>.md

Render report exactly as you specified.

T007 — Wire into pipeline ✅ keep, but fix stage IDs + map name

Update runStage.ts so:

S6_SELECT_TEMPLATES calls writeSelectionResult

S7_RENDER_DOCS calls writeRenderedDocs

And ensure the stage→gates wiring matches:

S6 → G4

S7 → G5

You don’t need a “GATE_STAGE_MAP” if your manifest already has stage_gates, but whichever you’re using, it must reflect the IDs above.

T008 — Test ✅ keep, but adjust “all 7 gates pass” expectation

“All 7 gates pass” is only true if the other MVP gates also have evidence present:

G1, G2, G3, G6, G8

If your run generator already writes those evidence files, great.
If not, your test should scope to:

S6 → G4 PASS

S7 → G5 PASS

Then separately validate the full run.

What I would change in your file placement (small, but makes the architecture cleaner)

You put S6/S7 writers in src/core/gates/evidence.ts.

Better separation:

src/core/templates/evidence.ts (selection + render outputs)

Gates should evaluate, not generate

If you already started gates/evidence.ts, it’s not fatal, but it will get messy fast.

Your “implementation-ready prompt” (cleaned and corrected)

If you’re feeding this to an internal agent, use this instruction set:

Implement S6_SELECT_TEMPLATES + G4

Implement S7_RENDER_DOCS + G5

Use registries/TEMPLATE_INDEX.json as index input

Guarantee G5 passes by building context from scanned placeholder keys (not heuristics)

Add json_eq op

Update GATE_REGISTRY + pipeline wiring

Output:

selection_result.json, selection_report.json

rendered_docs/*.md

render_report.json with unresolved_placeholders_count == 0

gate reports for G4 and G5

If you want to proceed without ambiguity: tell me what your current TEMPLATE_INDEX.json structure looks like (just the top-level keys + one template entry), and I’ll rewrite T003/T005 to match it exactly so you don’t waste a pass on schema mismatch.