Yes. Based on what’s actually in the zips (current repo scaffolding + stored runs) versus what your AXION docs define (control planes, agent kit, registries/contracts, knowledge library), here’s what the system is still missing.

1) Core pipeline stages are present but not fully real
Missing or not wired into the real run

These stages exist as concepts/modules, but your current axion run flow is still using placeholders for them:

INTAKE validation + normalization

You have the CLI surface for it, but it’s stubbed (NotImplemented) / not executed in the run pipeline.

Missing outputs in practice:

submission

normalized_input

submission_record

CANONICAL build

Canonical spec exists as a placeholder file today.

Missing in practice:

real canonical synthesis from normalized intake

unknowns/assumptions capture as a first-class artifact

canonical diffs between revisions/reruns

STANDARDS applicability + resolution

Standards snapshot file exists today, but it’s a placeholder.

Missing in practice:

applicability evaluation output (“why these apply”)

constraint pack resolution and conflict decisions

deterministic resolver trace recorded into the snapshot

TEMPLATE selection + fill

You have a template library and you are generating rendered docs inside runs, but the system is still missing the full formal contract end-to-end:

selection result artifact is not consistently treated as a contract input/output across stages

render envelopes and completeness enforcement need to be the source of truth (not “rendered_docs bloat”)

Bottom line: Your pipeline currently executes the back half reliably (plan → gates → proof → kit → state/verify), but the front half (intake/canonical/standards/templates) isn’t “real” yet.

2) Run Profiles / Build Recipes are not implemented as a deterministic selector

You’ve designed profiles, but the system still needs:

a RUN_PROFILE_REGISTRY (source-of-truth dataset)

profile resolution result artifact

effective config artifact (profile + mode overlay applied)

activation rules: which stages run, which gates, which proofs, which kit variant

Right now: runs can be started, but “what kind of run is this” is not enforced through an auditable profile resolution contract.

3) Registry + contract enforcement exists on paper, but runtime enforcement is incomplete

You’ve expanded registries/contracts, but the runtime still needs the “enforcement surface” to make them real:

Missing enforcement pieces

Schema validation at every stage boundary

the system needs to fail fast when artifacts do not validate (not just “continue and hope”)

Referential integrity checks

registry IDs referenced must exist (templates, standards, gates, proof types)

Compatibility checks

template versions ↔ standards snapshot versions ↔ schema versions ↔ gate profiles

If you don’t lock these as runtime checks, the system can drift while still “running.”

4) Gates are running, but gate governance isn’t fully locked

You’re producing gate reports, but the following “hardening” items are still missing or incomplete:

Predicate operator registry enforcement

gates must be restricted to known operators (no invented predicates)

Evidence policy mapping enforcement

“gate X requires proof types Y” must be enforced at evaluation time

Non-overridable gate list

must exist as a policy surface and be enforced consistently

Pointer resolver

evidence pointers must dereference and optionally hash-match

Right now you can produce gate artifacts; you still need “gates as law.”

5) Proof ledger exists, but proof completeness rules still need to be enforced

You’re writing a proof ledger, but the system still needs:

a registered set of proof types + required fields (enforced)

proof-to-gate linkage rules (enforced)

“PASS requires evidence per policy” wired into gate evaluation outcomes

redaction policies enforced on proof/log outputs (especially external kits)

Otherwise, proof becomes “nice-to-have” instead of “required to pass.”

6) Kit packaging works, but kit content minimization + external safety is not fully enforced

You’ve documented internal vs external kit rules, but system still needs:

deterministic kit variant filtering (external must exclude restricted knowledge/data)

enforced “kit must not contain secrets” scanner at packaging time

enforced allowlist behavior for reuse/plagiarism policy

kit validation step (KCP-side or kit validator) that must run before execution

Today: kit artifacts are produced; next is “safe and enforceable kits.”

7) Repo bloat control is missing (it will get worse)

From the repo scan focus:

most growth pressure is run outputs (rendered_docs inside .axion/runs)

without a retention + gitignore policy, AXION will keep bloating even if code stays clean

Missing:

retention rules (keep/prune)

“goldens live here, runtime runs live there” separation

rendered docs strategy (store only envelopes + hashes, not huge doc mirrors)

8) Operator Console integration is not finalized

You chose Option A, but you’re now reconsidering the separate Controller/.

What’s missing is one definitive execution bridge:

either:

a wrapper service (Controller or embedded), or

Axion hosting the local operator API directly

Until that’s locked, the App can’t reliably be the repo cockpit.

9) Conformance tests are not complete for the system itself

You have a test suite direction, but the system still needs:

golden fixtures per stage (intake/canonical/standards/templates/plan)

negative suites (“must fail at correct gate with remediation”)

end-to-end golden run fixture (full pipeline) that CI executes

determinism checks (normalize noise fields and diff)

Without these, it’s hard to prove AXION works the way the docs claim.

Summary: what’s missing in one line

You have the system skeleton and the back-half pipeline, but you still need to make the front-half real, then lock profile resolution + contract validation + evidence enforcement + retention so the system becomes deterministic and doesn’t drift or bloat.