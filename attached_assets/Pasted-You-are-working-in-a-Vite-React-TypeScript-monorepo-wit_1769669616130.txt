You are working in a Vite + React + TypeScript monorepo with a Node/Express server and shared Zod schemas.

GOAL
Perform a FULL rebrand + terminology refactor:
- Brand (UI/Product): Axiom
- System/API/Engine: Assembler
- Run -> Assembly
- Bundle -> Kit
- Handoff -> Delivery
- Remove ALL “roshi” strings/titles across the repo.

CRITICAL CONSTRAINTS
- Do not break functionality. Keep handlers working.
- Implement backward-compatible route aliases for 2–4 weeks (old routes still work).
- Keep existing upload functionality and extend upload routes to plural (POST /v1/uploads) and add GET /v1/uploads/:uploadId if missing.
- After changes, `rg "roshi|ROSHI|Roshi"` must return ZERO results.

========================================
STEP 1 — RENAME FILES/FOLDERS (use git mv)
========================================
Run these commands from repo root:

git mv scripts/roshi-init.mjs scripts/assembler-init.mjs
git mv scripts/roshi-generate.mjs scripts/assembler-generate.mjs
git mv scripts/roshi-seed.mjs scripts/assembler-seed.mjs
git mv scripts/roshi-draft.mjs scripts/assembler-draft.mjs
git mv scripts/roshi-review.mjs scripts/assembler-review.mjs
git mv scripts/roshi-verify.mjs scripts/assembler-verify.mjs
git mv scripts/roshi-lock.mjs scripts/assembler-lock.mjs
git mv scripts/roshi-package.mjs scripts/assembler-package.mjs
git mv scripts/roshi-package-workspace.mjs scripts/assembler-package-workspace.mjs

git mv roshi assembler

git mv docs/roshi_v2 docs/assembler_v1

git mv client/src/pages/home.tsx client/src/pages/create.tsx
git mv client/src/pages/runs.tsx client/src/pages/assemblies.tsx
git mv client/src/pages/docs.tsx client/src/pages/api-docs.tsx

========================================
STEP 2 — UPDATE package.json SCRIPTS
========================================
Replace ALL roshi:* scripts with assembler:* scripts:

"assembler:init": "node scripts/assembler-init.mjs",
"assembler:gen": "node scripts/assembler-generate.mjs",
"assembler:seed": "node scripts/assembler-seed.mjs",
"assembler:draft": "node scripts/assembler-draft.mjs",
"assembler:review": "node scripts/assembler-review.mjs",
"assembler:verify": "node scripts/assembler-verify.mjs",
"assembler:lock": "node scripts/assembler-lock.mjs",
"assembler:package": "node scripts/assembler-package.mjs",
"assembler:all": "npm run assembler:gen && npm run assembler:seed && npm run assembler:draft"

Update any docs mentioning `npm run roshi:*` -> `npm run assembler:*`.

========================================
STEP 3 — UPDATE SERVER ROUTES (server/v1-routes.ts)
========================================
Rename V1 API routes to Assembler naming.

A) Uploads
- POST /v1/upload  -> POST /v1/uploads
- Add GET /v1/uploads/:uploadId (if not already present)

B) Assemblies (was runs)
- POST /v1/runs -> POST /v1/assemblies
- GET  /v1/runs/:runId -> GET /v1/assemblies/:assemblyId

C) Kit (was bundle)
- GET /v1/runs/:runId/bundle     -> GET /v1/assemblies/:assemblyId/kit
- GET /v1/runs/:runId/bundle.zip -> GET /v1/assemblies/:assemblyId/kit.zip

D) Deliveries (was handoffs)
- POST /v1/runs/:runId/handoffs -> POST /v1/assemblies/:assemblyId/deliveries
- GET  /v1/runs/:runId/handoffs -> GET  /v1/assemblies/:assemblyId/deliveries
- GET  /v1/handoffs/:handoffId  -> GET  /v1/deliveries/:deliveryId
- POST /v1/handoffs/:handoffId/retry -> POST /v1/deliveries/:deliveryId/retry

E) Rename parameter usage
- req.params.runId -> req.params.assemblyId
- req.params.handoffId -> req.params.deliveryId

F) Rename reason code prefix
- Replace ALL `ROSHI_` reason codes to `ASSEMBLER_` (keep suffixes identical)

G) Rename any zip/script references
- roshi_bundle.zip -> axiom_kit.zip (or dynamic axiom_kit_<assemblyId>.zip for Content-Disposition)
- roshi-package-workspace.mjs -> assembler-package-workspace.mjs

H) Backward-compatible aliases (MUST)
Keep old endpoints working by registering old routes that call the same new handlers.
For each old route, add response headers:
- Deprecation: true
- Sunset: 2026-03-01
Optional:
- Link: </v1/assemblies>; rel="successor-version"

Alias mapping:
- /v1/runs -> /v1/assemblies
- /v1/runs/:runId -> /v1/assemblies/:assemblyId
- /v1/runs/:runId/bundle -> /v1/assemblies/:assemblyId/kit
- /v1/runs/:runId/bundle.zip -> /v1/assemblies/:assemblyId/kit.zip
- /v1/runs/:runId/handoffs -> /v1/assemblies/:assemblyId/deliveries
- /v1/handoffs/:handoffId -> /v1/deliveries/:deliveryId
- /v1/handoffs/:handoffId/retry -> /v1/deliveries/:deliveryId/retry
- /v1/upload -> /v1/uploads

========================================
STEP 4 — UPDATE SHARED TYPES + TABLES (shared/schema.ts)
========================================
Rename types and schemas:

Types:
- Run -> Assembly
- RunState -> AssemblyState
- RunStep -> AssemblyStep
- RunInput -> AssemblyInput
- RunBundle -> Kit
- Handoff -> Delivery
- HandoffState -> DeliveryState
- HandoffType -> DeliveryType

Schemas:
- createRunRequestSchema -> createAssemblyRequestSchema
- createHandoffRequestSchema -> createDeliveryRequestSchema

Tables:
Perform full rename (requires migration behavior):
- runs table -> assemblies
- handoffs table -> deliveries

Field renames:
- runId -> assemblyId
- handoffId -> deliveryId
- bundle -> kit

IMPORTANT:
Update all imports/usages throughout server code accordingly (v1-routes.ts, storage.ts, ai-generator.ts, workspace-manager.ts, adapters/*).

========================================
STEP 5 — UPDATE CLIENT ROUTING (client/src/App.tsx)
========================================
Implement new routes:
- /create -> Create wizard page
- /assemblies -> Assemblies list page
- /assemblies/:assemblyId -> Assembly detail page (use existing run detail component/page if present)
- /docs (or /api) -> API docs page (keep /docs if easier)

Add backward-compatible redirects:
- / -> redirect to /create
- /runs -> redirect to /assemblies
- /runs/:id -> redirect to /assemblies/:id

Also update UI labels:
- Any "Roshi" brand strings -> "Axiom"
- Any "Roshi system" -> "Assembler"
- Runs -> Assemblies
- Bundle -> Kit
- Handoffs -> Deliveries
- Generate -> Assemble

========================================
STEP 6 — UPDATE OTHER SERVER FILES FOR NAMING + PATHS
========================================
Update these as needed:
- server/workspace-manager.ts: storage paths data/runs -> data/assemblies; handoff/ -> delivery/; manifest -> assembly_manifest.json; bundle -> kit
- server/ai-generator.ts: replace “Roshi” -> “Axiom” (UI-facing); “Roshi system” -> “Assembler”; ROSHI_REPORT -> ASSEMBLER_REPORT
- server/signing.ts and webhook headers: if using X-Roshi-* headers, rename to X-Assembler-*
- server/file-upload.ts: update route strings to /v1/uploads and write extracted text to delivery/uploads/* in the kit

========================================
STEP 7 — FINAL SWEEP: ZERO “roshi” REMAINS
========================================
Run ripgrep checks and fix remaining hits:

rg -n "roshi|ROSHI|Roshi" .
rg -n "/v1/runs|/v1/handoffs|/v1/upload|/api/runs" server client shared
rg -n "roshi_bundle|roshi-package" .

All must be ZERO.

========================================
STEP 8 — SMOKE TEST
========================================
- npm run assembler:gen
- start server
- POST /v1/uploads
- POST /v1/assemblies
- GET /v1/assemblies/:assemblyId
- GET /v1/assemblies/:assemblyId/kit
- GET /v1/assemblies/:assemblyId/kit.zip
- POST /v1/assemblies/:assemblyId/deliveries

UI:
- / redirects to /create
- /runs redirects to /assemblies
- /docs loads and shows Assembler API + Axiom branding

DELIVERABLE
Commit all changes. The repo should compile and run. No “roshi” remains anywhere.
