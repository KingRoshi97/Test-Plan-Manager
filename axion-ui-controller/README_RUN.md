# AXION Build Runbook

## Project: axion-ui-controller

This build root was created by AXION kit-create. Follow these steps to build and run.

## Prerequisites

- Node.js 18+
- npm or pnpm
- PostgreSQL (if using database features)

## Build Pipeline

### 1. Run Docs Pipeline (generate → seed → draft → review → verify → lock)

```bash
npx tsx axion/scripts/axion-run.ts --build-root . --project-name axion-ui-controller --preset system --plan docs:full
```

### 2. Lock Documentation

```bash
npx tsx axion/scripts/axion-run.ts --build-root . --project-name axion-ui-controller --preset system --plan docs:release
```

### 3. Scaffold Application

```bash
npx tsx axion/scripts/axion-scaffold-app.ts --build-root . --project-name axion-ui-controller
```

### 4. Generate Build Plan

```bash
npx tsx axion/scripts/axion-build-plan.ts --build-root . --project-name axion-ui-controller
```

### 5. Run Tests

```bash
npx tsx axion/scripts/axion-test.ts --build-root . --project-name axion-ui-controller
```

### 6. Activate Build

```bash
npx tsx axion/scripts/axion-activate.ts --build-root . --project-name axion-ui-controller
```

### 7. Run Application

```bash
npx tsx axion/scripts/axion-run-app.ts
```

## Expected Artifacts

After completion, you should have:

- `axion-ui-controller/registry/` - Stage markers, verify report, lock manifest
- `axion-ui-controller/domains/` - Module documentation
- `axion-ui-controller/app/` - Application code
- `ACTIVE_BUILD.json` - Points to active build
- `manifest.json` - Build metadata

## Two-Root Model

- **System Root**: `./axion/` - Immutable scripts/templates/configs
- **Workspace Root**: `./axion-ui-controller/` - All generated outputs

⚠️ Never modify files in `axion/` after kit creation.

---
*Generated by AXION kit-create*
